name: testing

on:
  push:
    branches:
      - 'main'
  pull_request:
  workflow_dispatch: null

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      -
        name: Set up
        run: |
          sudo apt-get install libfuse2
      - name: Download LÖVE 💜
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          .github/workflows/love.sh
      - name: Tests
        run: |
          LOVE_BINARY=./love-12.0-x86_64.AppImage ./test.sh
